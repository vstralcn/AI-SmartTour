- 对外暴露的中间结果统一封装为 dataclass（`AgentStep`、`AgentExecution`），并通过 `steps` 列表向客户端推送可观测的执行轨迹。
- LLM 调用采用“先 fallback 文本再尝试流式”模式：优先返回 `execution.grounded_answer`，仅在配置齐全时走 `openai.AsyncOpenAI` 流式分片。
- 用户画像字段更新使用 `list(dict.fromkeys(...))` 去重合并，确保兴趣、同行人员等列表保持插入顺序且无重复。
- WebSocket 消息体遵循 `{type, content, done}` 三字段约定，由 `done: True` 标记一次完整响应结束。